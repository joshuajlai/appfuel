SELECT 
		r.role_id		AS id
,		r.role_name		AS name
,		r.role_code		AS authLevel
,		r.role_desc		AS description

FROM	roles AS r

INNER JOIN	role_paths as t 
ON			r.role_id = t.descendant

WHERE t.descendant = <?php $this->render('role_id') ?>
